# Standalone timing suite and local plotting notebook; the simulation stack
# (Torch, Gym, GeoPandas, etc.) is not required for the default four methods.
# Python 3.11/3.12 servers may reuse Anaconda extensions built for NumPy 1.x.
# NumPy 1.26 has no Python 3.13 wheels; newer Python needs a NumPy-2-compatible
# dependency stack in its own environment.
numpy>=1.26,<2; python_version < "3.13"
numpy>=2.1,<3; python_version >= "3.13"
docplex>=2.32
cplex>=22.1
-r requirements-mcmf.txt
threadpoolctl>=3.2
pandas>=2.1,<3
matplotlib>=3.8
jupyterlab>=4
nbclient>=0.10
ipykernel>=6
